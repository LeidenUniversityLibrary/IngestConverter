Checks for validity of Excel file and responds with any errors there might be.
If no errors are found, creates a usable UTF-8 encoded CSV file for batch ingest use.

Simply execute from the same folder where the Excel file exists.
If multiple Excel files exist, you will be prompted to choose one.
